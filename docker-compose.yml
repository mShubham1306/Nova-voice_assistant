version: '3.8'

services:
  web:
    build: .
    container_name: voiceassistant
    ports:
      - "5000:5000"
    environment:
      - ENVIRONMENT=development
      - DEBUG=True
      - SECRET_KEY=dev-secret-key-change-in-production
      - GEMINI_API_KEY=${GEMINI_API_KEY}
      - PORT=5000
      - HOST=0.0.0.0
    volumes:
      - ./backend:/app/backend
      - ./frontend:/app/frontend
    command: uvicorn backend.app:app --host 0.0.0.0 --port 5000

# For production, use:
# docker-compose -f docker-compose.prod.yml up
